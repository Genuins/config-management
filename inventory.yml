all:
  children:
    nginx:
      hosts:
        172.31.0.9:
          #ansible_user: ubuntu  # Correct user for EC2 instances running Ubuntu
          # ansible_ssh_private_key_file: ~/.ssh/id_rsa # Path to your private SSH key
    bastion:
      hosts:
        172.31.0.4: